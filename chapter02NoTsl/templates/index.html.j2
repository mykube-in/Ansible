<html>
  <head>
    <title>Welcome to ansible</title>
  </head>
  <body>
  <h1>nginx, configured by Ansible</h1>
  <p>If you can see this, Ansible successfully installed nginx.</p>

  <p>Running on {{ inventory_hostname }}</p>
  <p>
        ansible_managed = "{{ ansible_managed }}"       <br>
        template_host = "{{ template_host }}"           <br>
        template_uid = "{{ template_uid }}"             <br>
        template_path = "{{ template_path }}"           <br>
        template_fullpath = "{{ template_fullpath }}"   <br>
        template_run_date = "{{ template_run_date }}"   <br>
  </p>

  </body>
</html>

